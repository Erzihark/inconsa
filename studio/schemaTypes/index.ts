import type { SchemaTypeDefinition } from 'sanity'

// Content schemas are added in Phase 1 (siteSettings, project, client, service,
// galleryImage, machineCategory, machine + localized field types).
export const schemaTypes: SchemaTypeDefinition[] = []
